#!/bin/bash
# Direct PHP CLI wrapper
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export LD_LIBRARY_PATH="$PROJECT_ROOT/bin/lib:$LD_LIBRARY_PATH"
exec "$PROJECT_ROOT/bin/frankenphp" php-cli "$@"
